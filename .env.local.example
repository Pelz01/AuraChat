# Frontend Environment Variables for AuraChat

# Backend URL - This will be your Render backend URL when deployed
NEXT_PUBLIC_BACKEND_URL=http://localhost:3001

# For local development:
# 1. Start the backend: cd backend && npm start
# 2. Start the frontend: npm run dev
# 3. The frontend will connect to the backend at localhost:3001

# For production:
# 1. Deploy backend to Render with your OPENROUTER_API_KEY
# 2. Deploy frontend to Vercel with NEXT_PUBLIC_BACKEND_URL pointing to your Render URL
# 3. Example: NEXT_PUBLIC_BACKEND_URL=https://your-app-name.onrender.com 